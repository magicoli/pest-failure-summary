{
	"name": "magicoli/pest-failure-summary",
	"description": "PHPUnit/Pest extension: concise failure and skipped summary after the test run",
	"type": "library",
	"license": "AGPL-3.0-or-later",
	"autoload": {
		"psr-4": {
			"Magicoli\\PestFailureSummary\\": "src/"
		}
	},
	"require": {
		"phpunit/phpunit": "^10.0 || ^11.0 || ^12.0"
	}
}
